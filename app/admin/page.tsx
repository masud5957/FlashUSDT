import { AdminPanel } from '@/components/admin-panel'

export const metadata = {
  title: 'Admin Payment Configuration',
  description: 'Configure the static BEP-20 payment destination.',
}

export default function AdminPage() {
  return <AdminPanel />
}
